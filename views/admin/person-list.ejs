<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head")  %> 
</head>
<body>
    
    <%- include("../partials/nav")  %> 

    <div class="container mt-3">
          <div class="row">
            <div class="col-md-12 mt-3">
                <% if (action != "undefined" && action=="edit") { %>
                    <div class="alert alert-success">
                        <%= personid %> id numaralı  person Güncelledi.
                    </div>
                <% } %>
                <% if (action != "undefined" && action=="create") { %>
                    <div class="alert alert-success">
                        Person Eklendi.
                    </div>
                <% } %>
                <% if (action != "undefined" && action=="delete") { %>
                    <div class="alert alert-success">
                        Person Silindi.
                    </div>
                <% } %>
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th style="width: 50px;">id</th>
                            <th style="width: 150px;">resim</th>
                            <th>isim</th>
                            <th>kategori</th>
                            <th style="width: 130px;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% persons.forEach(person => { %>
                            <tr>
                                <td>#<%= person.dataValues.id %> </td>
                                <td>
                                    <img src="/static/images/<%= person.dataValues.resim %>" class="img-fluid" alt="">
                                </td>
                                <td>
                                    <%= person.dataValues.isim %> 
                                </td>
                                <td>
                                    <% person.categories.forEach(category => { %>
                                        <p> <%= category.name %> </p>
                                    <% }) %>
                                </td>
                                <td>
                                    <a class="btn btn-sm btn-primary" href="/admin/persons/<%= person.dataValues.id %>">edit</a>
                                    <a class="btn btn-sm btn-danger" href="/admin/person/delete/<%= person.dataValues.id %>">delete</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
          </div>
    </div>
</body>
</html>



